## Exercice

- Rename `my_library_sum` function to `sum` and use the namespace `my_library` and call it from main
- Use objdump to observe symbols, what do you observe?
- Now use modern c++ 17 to encapsulate `sum` into `math` namespace 