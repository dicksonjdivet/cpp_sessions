## Exercice

- compile the binary
- Using a tool such as objdump see the associated symbols
- Remove `_int` and `float` suffixes of both `print_*` functions.
Try to compile, what do you observe ?
- Remove the `extern "C"` clause and try to compile.
What happened ?
- Now use objdump to see the associated symbols, can you find both print functions ?
